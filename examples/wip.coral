# import cstd (
#   printf = c_printf,
#   malloc = c_malloc,
#   fopen = c_fopen,
#   fwrite = c_fwrite,
#   fread = c_fread,
#   fclose = c_fclose)

let cprintf = extern("c", "printf", Func[][Str, ...])

func foobar():
  return cprintf("Hello, %s\n", "World")

func fibonacci(n:Int64):
  if 1 + 1 = 3:
    cprintf "hi\n"
  else:
    cprintf "oops\n"
  return ()

foobar ()
fibonacci ()
