# import cstd (
#   printf = c_printf,
#   malloc = c_malloc,
#   fopen = c_fopen,
#   fwrite = c_fwrite,
#   fread = c_fread,
#   fclose = c_fclose)

let cprintf = extern("c", "printf", Func[][Str, ...])
cprintf("Hello, %s\n", "World")
