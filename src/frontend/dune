(ocamllex lexer)

(menhir
 ;; (flags --external-tokens Token --explain --comment)
 (flags --external-tokens Token)
 (modules grammar))

(library
 (name coral_frontend)
 (libraries unix coral_core)
 (preprocess
  (pps ppx_let ppx_compare ppx_deriving.show ppx_sexp_conv ppx_inline_test)))
